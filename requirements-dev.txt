# Additional dependencies for building the .exe and running the test suite.
# Install after requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pyinstaller==6.22.0
pytest==9.1.1
